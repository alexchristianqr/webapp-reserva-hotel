package core.utils;

//import static org.junit.jupiter.api.Assertions.assertEquals;
//
//import java.time.LocalDate;
//import java.time.format.DateTimeFormatter;
//import java.time.temporal.ChronoUnit;
//import org.junit.jupiter.api.Test;

class UtilTest {

//    @Test
//    void calcularEdadDevuelveLaEdadEsperada() {
//        LocalDate birthDate = LocalDate.of(2000, 1, 1);
//        String fechaNacimiento = birthDate.format(DateTimeFormatter.ofPattern("dd/MM/yyyy"));
//        String expected = String.valueOf(ChronoUnit.YEARS.between(birthDate, LocalDate.now()));
//
//        assertEquals(expected, Util.calcularEdad(fechaNacimiento));
//    }
}

